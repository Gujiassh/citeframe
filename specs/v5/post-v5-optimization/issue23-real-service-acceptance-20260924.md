# Issue 23 real-service editing acceptance

The feature stack extends the prerequisite real-service harness with `infra/testing/test_research_edit_services.py`. PostgreSQL migrates through the actual candidate head. A synthetic source is stored in real S3, and the real create route uses the default v3 release. Independent Worker processes execute automatic plan, research, unresolved-conflict policy and final publication. No human-decision API is called; both decisions must be submitted policy decisions with null actor.

The editor test validates an obsolete original hash before any edit exists, then races two independent PostgreSQL connections for expectedVersion=0 (first save) and expectedVersion=1 (CAS). Exactly one save wins each race, the other receives report_edit_version_conflict, and backend PIDs must differ. The service responses and subsequent production API GET prove the winning version/text and unverified status. A separately committed creator-membership revocation blocks a later API PUT without changing the edition. Generated original bytes/hash remain unchanged.

The concurrency races call the production service directly in independent sessions; permission/error and refresh checks use the production API via TestClient. This is not live browser, BFF transport, workspace-switch or draft-preservation UI evidence. The prerequisite crash/restore suite also runs on this feature HEAD with the default adaptive/policy release. The fixture provider adds nextQuery=null only when the actual frozen schema requires it; historical v2 behavior is not replaced.

Local development run on 2026-09-24: test passed using real PG17 and isolated disk-backed SeaweedFS S3, with two distinct PostgreSQL PIDs for each 200/409 race. Exact committed-HEAD and Actions evidence remains required. The initial shared S3 new-bucket PUT failed with HTTP500; a separate local S3 process/data directory was used without stopping or changing shared resources. Credentials remain only in local configuration references.

The revocation race now holds the Run row lock in the revoker connection, proves via pg_blocking_pids that the saver is blocked on that exact connection, commits membership removal, and checks the waiting saver returns research_permission_denied/403 with no edition change. The later API PUT returns404. Per-race snapshots include the Research domain and edition rows; requests, outcomes and physical connection IDs are retained. This does not assert physical database-byte equality.

